<!--pages/chapter_page/chapter_page.wxml-->
<view id='root'>
  <chapteritem wx:for="{{chapters}}" catalog="{{item}}" wx:key="{{item}}">
  </chapteritem>
  <error wx:if="{{showError}}"></error>
</view>